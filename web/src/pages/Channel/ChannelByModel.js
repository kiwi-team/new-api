import ChannelsTable from '../../components/table/ChannelsByModelNameTable';

const ChannelByModel = () => {
  return (
    <div className='mt-[64px] px-2'>
      <ChannelsTable />
    </div>
  );
};

export default ChannelByModel;
